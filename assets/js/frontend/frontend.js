(function ($, window) {    'use strict';    $('.quiz-submission-form').on('submit', function (e) {        e.preventDefault();        const data = $(this).serialize();        const ajax_data = {            action: 'qp_quiz_submission',            data: data        };        $.post(qp_quiz_submission.ajax_url, ajax_data, function (response) {            $('.result').html("").append(response);        });    });})(jQuery, window);